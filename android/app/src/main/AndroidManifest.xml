<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- neverForLocation: app KHÔNG dùng kết quả quét BLE để suy ra vị trí, chỉ
         nhận diện thiết bị xe → khai báo để tránh phải xin quyền vị trí (Play). -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        tools:targetApi="s"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Đã GỠ REQUEST_IGNORE_BATTERY_OPTIMIZATIONS: Google Play hạn chế quyền này
         (kèm hộp thoại cấp trực tiếp). App vẫn ĐỌC được trạng thái tối ưu pin qua
         PowerManager.isIgnoringBatteryOptimizations (không cần quyền), và HƯỚNG DẪN
         người dùng tới màn hình Cài đặt tối ưu pin (ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS)
         để tự tắt — đây là cách hợp lệ với chính sách Play. Logic chạy nền không đổi. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- READ_EXTERNAL_STORAGE đã được gỡ: app chỉ lưu file trong vùng riêng
         (getApplicationDocumentsDirectory / getTemporaryDirectory), không đọc
         bộ nhớ ngoài. Gỡ quyền này giúp dễ qua kiểm duyệt Play hơn. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:label="Chào Xe Tự Động"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:hardwareAccelerated="true">

        <!-- ===== MAIN ACTIVITY ===== -->
        <activity
            android:name="com.hicar.ora.limited.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Cho phép khởi chạy khi máy đang khóa (Direct Boot) -->
        <receiver
            android:name="com.hicar.ora.limited.BootReceiver"
            android:exported="true"
            android:enabled="true"
            android:directBootAware="true"> 
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ===== BLUETOOTH RECEIVER ===== -->
        <receiver
            android:name="com.hicar.ora.limited.BluetoothReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
                <action android:name="android.bluetooth.device.action.FOUND" />
                <action android:name="android.bluetooth.adapter.action.DISCOVERY_FINISHED" />
            </intent-filter>
        </receiver>

        <!-- Cho phép phát nhạc khi máy đang khóa (Direct Boot) -->
        <service
            android:name="com.hicar.ora.limited.AudioForegroundService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback|specialUse"
            android:directBootAware="true"
            android:stopWithTask="false">
            <!-- specialUse: cho phép khởi động service từ BOOT_COMPLETED trên Android 15
                 (mediaPlayback bị cấm start từ boot ở API 35). Luồng thường + Android Auto
                 vẫn dùng mediaPlayback nên không ảnh hưởng. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Automotive greeting audio playback at device boot" />
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- ===== FLUTTER OVERLAY WINDOW SERVICE ===== -->
        <service 
            android:name="flutter.overlay.window.flutter_overlay_window.OverlayService" 
            android:exported="false" 
            android:foregroundServiceType="specialUse">
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" 
                android:value="Floating control bubble that lets the driver play or stop the greeting audio while using navigation on the car screen"/>
        </service>

        <!-- ===== FLUTTER EMBEDDING ===== -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ===== ANDROID AUTO ===== -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Cho phép truy vấn ContentProvider trạng thái kết nối của Android Auto
             (CarConnection) trên Android 11+ (package visibility). Thiếu khai báo này
             thì contentResolver.query trả về null → không phát hiện được AA. -->
        <provider android:authorities="androidx.car.app.connection" />
    </queries>

</manifest>
